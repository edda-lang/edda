
module tls.main

import std.io.stream
import local.error

public function main(out: Stdout) -> ()
    with {out}
{
    let _ = error.TlsError.bad_record
    stream.print_line(out, "tls v0.1.0: record + handshake + client land here")
}
