
module driver.exit

import diagnostics.emit.collection as diags_mod
import diagnostics.emit.severity as severity_mod

public stable function exit_code_of(diags: diags_mod.Diagnostics) -> i32 {
    return severity_mod.exit_code(diags_mod.worst_severity(diags))
}
