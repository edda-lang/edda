module x64.emit.types

import local.gen.sel.regs as regs_mod


public type BranchFixup {
    site: u32
    target_block: u32
}

public type GpOperand {
    case reg(r: regs_mod.Gp64Reg)
    case imm32(v: i32)
}
