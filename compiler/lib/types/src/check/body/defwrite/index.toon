loc: src/check/body/defwrite

modules[1]{name,file,line,visibility}:
  defwrite,defwrite.ea,1,public

effects[1]{id,row}:
  0,"allocator, err: alloc.AllocError, divergence"

functions[1]{name,file,line,end,visibility,stability,sig,eff,cone,calls}:
  defwrite.write_at_def_id,defwrite.ea,10,30,public,,"(local_types: mutable Vec_TyId.Vec, def_id: def_mod.DefId, ty: ty_mod.TyId, allocator: Allocator) -> ()",0,=,Vec_TyId.len | Vec_TyId.push | Vec_TyId.set_at | def_mod.index | ty_mod.error

