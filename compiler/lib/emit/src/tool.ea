
module emit.tool

derive eq, hash, ord, clone, debug for Tool


public type Tool {
    case llc
}


public stable function tool_name(t: Tool) -> String {
    return match t {
        case .llc => "llc"
    }
}
