module optimize.alias_mem.store.flow.model

import mir.id.local as mir_local_mod


public function same_local(a: mir_local_mod.LocalId, b: mir_local_mod.LocalId) -> bool {
    return mir_local_mod.index(a) == mir_local_mod.index(b)
}
