
module std.os.fs.readdir.error

public type ReadDirError {
    case not_found(path: String)
    case permission_denied(path: String)
    case not_a_directory(path: String)
    case invalid_utf8
    case os_error(code: i32)
    case other
}
