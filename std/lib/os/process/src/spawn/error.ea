
module std.os.process.spawn.error

public type SpawnError {
    case executable_not_found(path: String)
    case not_in_allowlist(exe: String)
    case cwd_outside_scope(cwd: String)
    case spawn_failed(reason: String)
}
