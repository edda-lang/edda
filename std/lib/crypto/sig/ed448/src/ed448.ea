
module std.crypto.sig.ed448


public type Ed448SigError {
    case invalid_signature_encoding
    case invalid_public_key_encoding
    case internal_error
}


public function public_from_private(
    private_seed: [u8] where private_seed.len() == 57,
    public_out: mutable [u8] where public_out.len() == 57,
) -> ()
    extern "__edda_ed448_public_from_private"


public function sign(
    private_seed: [u8] where private_seed.len() == 57,
    message: [u8],
    sig_out: mutable [u8] where sig_out.len() == 114,
) -> ()
    extern "__edda_ed448_sign"


public function verify(
    public_key: [u8] where public_key.len() == 57,
    message: [u8],
    signature: [u8] where signature.len() == 114,
) -> bool
    with {err: Ed448SigError}
    extern "__edda_ed448_verify"


public function sign_with_context(
    private_seed: [u8] where private_seed.len() == 57,
    message: [u8],
    context: [u8] where context.len() >= 1 && context.len() <= 255,
    sig_out: mutable [u8] where sig_out.len() == 114,
) -> ()
    extern "__edda_ed448_sign_ctx"

public function verify_with_context(
    public_key: [u8] where public_key.len() == 57,
    message: [u8],
    context: [u8] where context.len() >= 1 && context.len() <= 255,
    signature: [u8] where signature.len() == 114,
) -> bool
    with {err: Ed448SigError}
    extern "__edda_ed448_verify_ctx"


public function sign_prehashed(
    private_seed: [u8] where private_seed.len() == 57,
    prehash: [u8] where prehash.len() == 64,
    context: [u8] where context.len() <= 255,
    sig_out: mutable [u8] where sig_out.len() == 114,
) -> ()
    extern "__edda_ed448_sign_ph"

public function verify_prehashed(
    public_key: [u8] where public_key.len() == 57,
    prehash: [u8] where prehash.len() == 64,
    context: [u8] where context.len() <= 255,
    signature: [u8] where signature.len() == 114,
) -> bool
    with {err: Ed448SigError}
    extern "__edda_ed448_verify_ph"
