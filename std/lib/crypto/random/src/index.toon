loc: src

modules[1]{name,file,line,visibility}:
  random,random.ea,1,public

effects[1]{id,row}:
  0,random

functions[1]{name,file,line,end,visibility,stability,sig,eff,cone,calls}:
  random.fill,random.ea,4,6,public,,"(random: Random, buf: mutable [u8]) -> ()",0,=,""

